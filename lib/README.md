# Lib

This folder is reserved for local project libraries.

Version 4 currently does not require vendored libraries here. External
dependencies are declared in `platformio.ini` through `lib_deps` so PlatformIO
can install the correct packages automatically.
